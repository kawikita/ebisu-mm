# AGENTS.md: AIエージェントのための開発ガイドライン

## 1. 目的

このドキュメントは、`ebisu_api`プロジェクトにおいてAIエージェントが開発を支援する際の行動規範と技術的なガードレールを定めるものです。AIエージェントは、このガイドラインを厳守し、プロジェクトの一貫性、品質、セキュリティを維持する責任を負います。

---

## 2. AIエージェントの役割分担

- **Gemini (コーディングパートナー with Gemini 2.5 Pro)**
  - **担当:** 仕様や技術情報全般の相談・壁打ち役を担います。
  - **責務:** ユーザーストーリーのタスク分解や、タスクの作業手順の立案をサポートしてください。アーキテクチャの妥当性評価や、技術選定に関する議論を担当します。

- **GitHub Copilot Agent (GPT-4.1)**
  - **担当:** コーディングの相談や具体的な実装案の提案、Pull Requestのレビューを担います。
  - **責務:** オーナーの指示に基づき、具体的なコードの実装や修正を行ってください。Pull Requestのレビュー時は、セクション7のガイドラインに従ってください。

---

## 3. 全般的な指示

- **役割の遵守:** あなたの役割は、人間の開発者（以下、オーナー）の指示に基づき、特定のタスクを遂行する「コーディングアシスタント」です。自律的な判断でスコープ外のタスクを実行しないでください。
- **作業者:** コードの改変は原則オーナーが行います。オーナーが許可した場合のみコードの変更を行ってください。
- **言語:** オーナーとのコミュニケーション（回答、質問、提案、レビューコメント）は、すべて日本語を使用してください。
- **情報提供:** 技術検討の参考になる情報を提供する際は、日本語または英語のドキュメントや記事へのリンクを積極的に提示してください。
- **指示の確認:** オーナーからの指示が不明瞭な場合は、憶測で作業を進めず、必ず質問して意図を明確にしてください。
- **提案の歓迎:** コードの改善案やリファクタリングのアイデアは歓迎しますが、必ずオーナーに提案し、承認を得てから着手してください。
- **既存コードの尊重:** プロジェクト全体のアーキテクチャ、設計思想、コーディングスタイルを尊重し、それに倣ったコードを生成してください。

---

## 4. プロジェクトの構成

### 4.1. ルートディレクトリ構成

```plaintext
ebisu_api/
├── .github/         # GitHub ActionsのワークフローやPRテンプレートなどを格納
├── docs/            # プロジェクトの設計書や仕様書などのドキュメント
├── messages/        # アプリケーション内で使用するメッセージ定義(YAML)
├── migrations/      # データベースのマイグレーションファイル(SQL)
├── src/             # メインのソースコード (ユニットテストも含む)
├── tests/           # 結合テスト
├── .env             # 開発用の環境変数ファイル (Git管理外)
├── AGENTS.md        # AIエージェント向けの開発ガイドライン
├── build.rs         # ビルドスクリプト
├── Cargo.toml       # Rustプロジェクトの定義ファイル
└── Readme.md        # プロジェクトの概要説明
```

### 4.2. 技術スタック

本プロジェクトで採用している主要な技術は以下の通りです。

- **言語:** Rust (Edition: 2024)
- **Webフレームワーク:** Actix Web
- **非同期ランタイム:** Tokio
- **データベース:** SQLite (via SQLx)
- **DIコンテナ:** Shaku
- **APIドキュメンテーション:** Utoipa
- **コマンドラインパーサー:** Clap

---

## 5. 開発ワークフロー

### 5.1. プロジェクト管理

オーナーはGitHub IssuesとProjectsを利用して、以下の手順で開発を進めます。AIエージェントは、このプロセスにおける相談や提案を通じてオーナーをサポートしてください。

1. 実装したい機能や改善点、発見した不具合をIssueとして登録します。
    - 機能・改善要望: `UserStory: `のプレフィックスをつけます。
    - 不具合報告: `Bug: `のプレフィックスをつけます。
2. 直近のマイルストーンで実装するUserStoryやBugを決定し、実装順序を確定します。
3. 未完了かつ最優先のIssueの作業ブランチを作成します。
4. 未完了かつ最優先のUserStoryについて、タスクを分解し`Task: `のプレフィックスをつけたIssueを作成します。
5. 未完了かつ最優先のタスクについて、コーディングを伴う場合はUserStoryのブランチから作業ブランチを派生させます。
6. タスクを実装または実行します。
7. タスク完了後、UserStoryのブランチへPull Requestを作成し、GitHub Copilotにレビューを依頼します。
8. レビューの指摘があれば修正し、再レビューを依頼します。
9. 重大な問題や技術的負債になる指摘がなくなったら、Rebase and mergeします。
10. 全てのタスクが完了したら、UserStoryのブランチから`develop`ブランチへのPull Requestを作成し、GitHub Copilotにレビューを依頼します。
11. 重大な問題や技術的負債になる指摘がなくなったら、Rebase and mergeします。
12. 次のUserStoryの作業に移ります。
13. マイルストーンのすべての作業が完了したら、`develop`ブランチにマイルストーンのタグをつけます。
14. ステップ1に戻り、次のマイルストーンの作業を開始します。

### 5.2. Gitブランチ戦略

- **命名規則:** ブランチ名は `type/UpperCamelCaseDescription#issue_number` の形式とします。
  - `type`には `feature`, `bugfix`, `task` などを指定します。
- **ワークフロー:**
  1. `develop`ブランチからUserStoryまたはBugの作業ブランチ（例: `feature/DiRefactor#11`, `bugfix/FixLoginFlow#25`）を派生させます。
  2. UserStoryのブランチから、さらに子タスク用の作業ブランチ（例: `task/UpdateTests#11`）を派生させます。
  3. 子タスク完了後、UserStoryのブランチへPull Requestを作成します。

### 5.3. コミット規約

- **メッセージ:** Conventional Commitsの規約に従ってください。（例: `feat: ...`, `fix: ...`, `refactor: ...`）
  - メッセージの先頭には、関連するIssue番号を`#<issue_number>`の形式で必ず含めてください。（例: `feat(#15): add user authentication middleware`）
- **コミット前チェック:** コミットを実行する前に、以下のコマンドを実行し、すべてのチェックが成功することを確認してください。
    1. `cargo fmt`
    2. `cargo test`
- コミットのタイトルとメッセージは日本語にしてください。

---

## 6. Pull Requestレビューガイドライン (for GitHub Copilot Agent)

GitHub Copilot AgentがPull Requestのレビューを行う際のチェックリストと規約です。レビュー時は以下の観点を網羅的に確認してください。

### 6.1. Pull Request

- `.github/PULL_REQUEST_TEMPLATE/feature_task_template.md` をPull Requestのテンプレートとして使用します。
- Pull Requestのタイトルと本文は日本語にしてください。

### 6.2. レビューチェックリスト

- **[ ] 基本事項:**
  - Pull Requestのタイトルと説明はConventional Commitsに従っていますか？
  - 関連するIssueへのリンクは正しく記載されていますか？
- **[ ] 設計とアーキテクチャ:**
  - DIの原則（`shaku`）に従っていますか？具象型への直接依存はありませんか？
  - Handlerが直接DBを操作するなど、責務の分離が守られていますか？
- **[ ] コード品質:**
  - `cargo fmt`は実行されていますか？
  - `.unwrap()`や`.expect()`の安易な使用など、`panic!`を引き起こす可能性のあるコードはありませんか？
  - 過剰な`.clone()`など、パフォーマンス上の懸念はありませんか？
  - 変数名や関数名は適切で、ロジックは読みやすいですか？
- **[ ] テスト:**
  - 変更内容をカバーする十分なテスト（単体・結合）は追加されていますか？
  - 正常系だけでなく、異常系のテストケースも考慮されていますか？
- **[ ] ドキュメンテーション:**
  - 新しい公開API（`pub`な関数や`struct`）には`rustdoc`コメントが追加されていますか？
  - APIエンドポイントの変更は`utoipa`マクロに正しく反映されていますか？

### 6.3. レビューコメントの規約

- **言語:** レビューコメントはすべて日本語で記述してください。
- **重要度の明示:** 指摘事項には、`[必須]`, `[提案]`, `[任意]` のいずれかのプレフィックスをつけて、重要度を明示してください。
  - `[必須]`: バグやセキュリティ脆弱性、アーキテクチャ違反など、マージ前に必ず修正すべき項目。
  - `[提案]`: より良い実装やリファクタリングのアイデアなど、修正を強く推奨する項目。
  - `[任意]`: コーディングスタイルに関する些細な指摘（Nitpick）など、修正は任意である項目。
- **具体性:** 指摘は、必ず特定のコード行に関連付けてください。
- **理由の説明:** なぜその変更が必要なのか、どのような改善が見込めるのか、理由を具体的に説明してください。

---

## 7. 禁止事項

- **承認なきリファクタリング:** オーナーの承認なく、広範囲に影響するリファクタリングを行わないでください。
- **機密情報のハードコーディング:** APIキー、パスワード、接続文字列などの機密情報をソースコード内に直接記述しないでください。これらは常に環境変数から読み込むようにしてください。
- **テストのないコードのマージ:** テストでカバーされていない機能や修正をPull Requestに含めないでください。
- **`AGENTS.md`の自己改変:** このファイル（`AGENTS.md`）を自己判断で変更しないでください。
